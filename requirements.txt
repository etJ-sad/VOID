# VOID Framework Requirements
# Python 3.10+ (optimized for Python 3.11-3.13)

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Data Models
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Hardware Detection
psutil>=5.9.8

# AI/ML - Updated for Python 3.13 compatibility
numpy>=1.26.4
scikit-learn>=1.4.0
scipy>=1.12.0

# Deep Learning Models (optional - for large AI models)
# PyTorch - Required for loading large models (LLM, transformers)
# 
# For CPU-only (simpler, smaller):
#   pip install torch torchvision torchaudio
#
# For CUDA support (GPU acceleration - check your CUDA version first):
#   Visit https://pytorch.org/get-started/locally/ and select your system
#   Example for CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Note: Do NOT uncomment the line below - PyTorch installation varies by system
# torch>=2.1.0

# Large AI Models (Hugging Face) - OPTIONAL, for advanced analysis
# transformers>=4.35.0  # Required for large models (LLM, time-series)
# huggingface-hub>=0.19.0  # Required for downloading models (first time only)
# accelerate>=0.25.0  # Optional: for faster inference
# bitsandbytes>=0.41.0  # Optional: for quantized models (smaller memory)
# 
# Note: VOID works fine without these - uses sklearn models by default
# Install only if you want to use large AI models (phi-1.5, phi-2, etc.)

# YAML Processing
PyYAML>=6.0.1

# Templating
Jinja2>=3.1.3

# PDF Generation (optional)
reportlab>=4.1.0
# weasyprint>=61.0  # Uncomment if you want HTML to PDF conversion

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0

# Utilities
python-dateutil>=2.9.0

